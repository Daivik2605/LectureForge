# Backend

FastAPI service for upload, processing, and result retrieval.

Quick notes:
- Copy `backend/.env.example` to `backend/.env`.
- Run from repo root: `uvicorn backend.app.main:app --reload`
- Outputs are written under `data/` and `storage/` at the repo root in local dev.
